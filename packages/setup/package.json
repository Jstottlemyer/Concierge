{
  "name": "@concierge/setup",
  "version": "0.0.0",
  "private": true,
  "description": "Concierge first-run install orchestrator CLI (concierge-setup).",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "concierge-setup": "dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "probe-timing": "tsx scripts/probe-timing-harness.ts"
  },
  "dependencies": {
    "@concierge/core": "workspace:*",
    "@modelcontextprotocol/sdk": "latest"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
